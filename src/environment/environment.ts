export const environment = {
  production: false,
  // Base API URL — must include protocol (https://) and no trailing slash.
  // Services should use relative paths like '/api/...' and the interceptor will prepend this base.
  apiUrl: 'https://metra-backend.vercel.app'
};
